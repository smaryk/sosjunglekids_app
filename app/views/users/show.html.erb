<% provide(:title, @user.first_name + " " + @user.last_name) %>
<br><br><br><br>

<h2>User Profile for '<%= @user.first_name %> <%= @user.last_name %>'</h2>



<div class="row">
<% if @user.newsposts.any? %>
  <h3>Newsposts (<%= @user.newsposts.count %>)</h3>
  <br>
    <%= render @newsposts %>
  
    <%= will_paginate @newsposts %>
<% end %>

</div>
